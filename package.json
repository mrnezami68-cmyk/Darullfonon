{
  "name": "darullfonon-mvp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "worker:typecheck": "tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --lib ES2022 --types @cloudflare/workers-types worker/src/index.ts",
    "worker:db:local": "wrangler d1 migrations apply darullfonon --local --config wrangler.toml",
    "worker:dev": "wrangler dev --config wrangler.toml"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260901.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.128.0"
  }
}
